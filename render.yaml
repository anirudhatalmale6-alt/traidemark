services:
  - type: web
    name: traidemark
    runtime: node
    buildCommand: npm install --include=dev && npx vite build
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: ANTHROPIC_API_KEY
        sync: false
